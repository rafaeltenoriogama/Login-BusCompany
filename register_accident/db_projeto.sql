CREATE TABLE db_projeto (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50),
    carro VARCHAR(50),
    tipo VARCHAR(50),
    observacao TEXT,
    VeiculoTerceiro VARCHAR(20)
);

/* Tabela com requisitos mínimos para funcionar a página de registro de acidentes. */
